import trashSearch from '../assets/trash-search.jpeg';

export const NotFoundPage = () => {
  return (
    <div>
        <h2>
            Oh no! We can't find the page you are looking for.
        </h2>
        <p>try one of our links?</p>
        <img src={trashSearch} alt='page not found - man searching inside bin'/>esn't exist
    </div>
    
  )
}
